# Providers the nightly drift check cannot probe from GitHub's cloud IPs — they
# FAIL in CI even when healthy. The drift job alerts only on providers NOT in
# this list. Keep it honest: remove an entry if a provider becomes reachable
# (a stale entry here silently masks real drift for that provider). Full
# unblocked coverage stays the operator machine's job (local doctor / ops plane).
#
# Calibrated 2026-07-02 (run 28560807151):
# — no API key available on CI (auth-required, not drift):
datagolf
twitter
# — geo/bot-blocked from cloud IPs (403 / bot challenge / edge 503):
betfair
betr
entain
sportsbet
tab
unibet
# Added 2026-08-05 (run 30943130613). Pinnacle's `guest.api.arcadia` host started
# 403ing GitHub's runners on /sports/{id}/leagues while /enums still answers, so
# only the pinnacle.sports group trips. Verified healthy from a residential IP the
# same day (200, via `sportsdata-mcp doctor`) — a cloud-IP block, not drift.
pinnacle
