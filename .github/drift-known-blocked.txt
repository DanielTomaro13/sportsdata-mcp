# Providers that geo/bot-block GitHub's cloud IPs — they FAIL in the nightly
# drift workflow even when healthy. The drift job alerts only on providers NOT
# in this list. Keep it honest: remove an entry if a provider becomes reachable
# (a stale entry here silently masks real drift for that provider).
#
# Calibrated from the first workflow_dispatch run — see .github/workflows/drift.yml.
